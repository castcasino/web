export default defineEventHandler((event) => {
    /* Initialize locals. */
    let form

    /* Build form. */
    form = {
        type: 'form',
        title: 'Game Creator',
        url: 'https://cast.casino/creator',
    }

    /* Return statistics. */
    return stats
})
